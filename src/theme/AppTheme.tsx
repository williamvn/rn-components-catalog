import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    textHeader: {
        fontSize: 30,
        color: "black"
    },
    label: {
        color: "#949292"
    },
    centerContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
      },
});